<?php
// temporary source code repository || the url can be changed dynamicaly during updates
define('PUBLIC_SOURCE_CODE_URL_TARGET', 'http://pablolabeque.com/public_source_code');
define('ROOT_PATH', $_SERVER["DOCUMENT_ROOT"]);

function is_deployed() : bool
{
    if($_SERVER['SERVER_NAME'] == 'localhost')
    {
        return false;
    }else if($_SERVER['SERVER_NAME'] != 'localhost')  
    {
        return true;
    }
}
?>
